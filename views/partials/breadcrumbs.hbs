{{#if breadcrumbs}}

    <nav>
        <div class="nav-wrapper">
            <div class="col s12">
                {{#each breadcrumbs.items}}
                1
                    <a href="{{url}}" class="breadcrumb">{{title}}</a>
                {{/each}}
            </div>
        </div>
    </nav>

{{/if}}