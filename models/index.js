const Course = require('../models/course')
const User = require('../models/user')
const Order = require('../models/order')
const SliderMain = require('../models/slider')

const models = {
    Course,
    User,
    Order,
    SliderMain
}

module.exports = models